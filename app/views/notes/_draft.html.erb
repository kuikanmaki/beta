<li>  
  <span class="content">   
        <p><br><%= raw(note.content) %></p>
        <span class="timestamp"><%= time_ago_in_words(note.created_at) %> ago</span>
        <% if current_user?(note.user) %>
          <%= link_to "delete", note, method: :delete,
                                     data: { confirm: 'You sure?' },
                                     title: note.content %>
        <% end %>   
      <hr>
  </span>      
</li>