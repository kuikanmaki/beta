<fieldset>
	<h3>Start a new video chat</h3>

<%= form_for(@new_room) do |f| %>
	Party Name: <%= f.text_field :name %><br/><br/>
	Public? <%= f.check_box :public %><br/><br/>
	
	<%= f.submit %>
<% end %>
</fieldset>