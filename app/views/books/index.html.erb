<h1>Listing books</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Author</th>
    <th>Link</th>
    <th>Page</th>
    <th>Interest</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= book.name %></td>
    <td><%= book.author %></td>
    <td><%= book.link %></td>
    <td><%= book.page %></td>
    <td><%= book.interest %></td>
    <td><%= link_to 'Show', book %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book', new_book_path %>
