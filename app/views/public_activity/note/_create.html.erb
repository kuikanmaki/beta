added note

<% if activity.trackable %> 
  "<%= link_to activity.trackable.title, activity.trackable %>" to <%= link_to activity.trackable.page.name, activity.trackable.page %>
<% else %>
  which has since been removed
<% end %>  